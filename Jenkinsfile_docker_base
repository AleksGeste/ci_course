pipeline {
    agent any
    stages { 
        stage('build') {
            steps {
                // echo 'Hello World'
                bash -c "docker build -t api_test_base . -f Dockerfile_base"
            }
        }
    }
}
